<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo App - <?php echo $title ?? 'My Tasks'; ?></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        html, body {
            height: 100%;
        }
        
        body {
            display: flex;
            flex-direction: column;
        }
        
        .container {
            flex: 1 0 auto;
            padding-bottom: 20px;
        }
        
        footer {
            flex-shrink: 0;
            width: 100%;
        }
        
        :root {
            --primary-color: #6c63ff;
            --secondary-color: #4d44db;
            --light-color: #f8f9fa;
            --dark-color: #343a40;
        }
        
        body {
            background-color: #f5f5f5;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .navbar-brand {
            font-weight: 700;
            color: var(--primary-color) !important;
        }
        
        .task-item {
            transition: background-color 0.2s ease;
        }
        
        .task-item:hover {
            background-color: #f8f9fa;
        }

        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }
        
        .btn-primary:hover {
            background-color: var(--secondary-color);
            border-color: var(--secondary-color);
        }
        
        .completed-task {
            text-decoration: line-through;
            color: #6c757d;
        }
        
        .task-actions .btn {
            padding: 0.25rem 0.5rem;
            font-size: 0.875rem;
        }
        
        .filter-btns .btn {
            margin-right: 5px;
        }
        
        /* Enhanced Logo styles - REDUCED SIZE */
        .navbar-logo {
            height: 28px; /* Reduced from 55px */
            width: auto;
            margin-right: 10px; /* Reduced from 20px */
            border-radius: 4px;
            object-fit: contain;
        }
        
        .brand-container {
            display: flex;
            align-items: center;
            height: 100%;
        }
        
        .brand-text {
            font-size: 1.2rem; /* Reduced from 1.8rem */
            font-weight: bold;
            color: #fff !important;
            padding-left: 8px; /* Reduced from 15px */
            border-left: 1px solid rgba(255, 255, 255, 0.3); /* Reduced from 2px */
        }
        
        /* Make navbar shorter - REDUCED HEIGHT */
        .navbar {
            min-height: 35px; /* Reduced from 70px */
            padding: 5px 0; /* Reduced from 10px 0 */
        }
        
        .navbar-nav .nav-link {
            font-size: 0.9rem; /* Reduced from 1.1rem */
        }
        
        /* For smaller screens */
        @media (max-width: 768px) {
            .navbar-logo {
                height: 22px; /* Reduced from 45px */
                margin-right: 5px; /* Reduced from 10px */
            }
            
            .brand-text {
                font-size: 1rem; /* Reduced from 1.5rem */
                padding-left: 5px; /* Reduced from 10px */
            }
            
            .navbar {
                min-height: 30px; /* Reduced from 60px */
            }
        }
        
        /* Extra small screens */
        @media (max-width: 576px) {
            .navbar-logo {
                height: 20px; /* Reduced from 40px */
                margin-right: 4px; /* Reduced from 8px */
            }
            
            .brand-text {
                font-size: 0.9rem; /* Reduced from 1.3rem */
                padding-left: 4px; /* Reduced from 8px */
            }
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3 py-1"> <!-- Reduced mb-4 to mb-3 and py-2 to py-1 -->
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="index.php">
                <div class="brand-container">
                    <!-- Logo Image from images folder -->
                    <img src="images/sunbeam.jpg" 
                         alt="Sunbeam Institute Logo" 
                         class="navbar-logo">
                    <!-- App Name -->
                    <span class="brand-text">TodoApp</span>
                </div>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <?php if (isLoggedIn()): ?>
                    <li class="nav-item">
                        <span class="nav-link text-light" style="padding: 0.2rem 0.5rem;">Hello, <?php echo htmlspecialchars(getCurrentUsername()); ?></span> <!-- Reduced padding -->
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="logout.php" style="padding: 0.2rem 0.5rem;">Logout</a> <!-- Reduced padding -->
                    </li>
                    <?php else: ?>
                    <li class="nav-item">
                        <a class="nav-link" href="login.php" style="padding: 0.2rem 0.5rem;">Login</a> <!-- Reduced padding -->
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="register.php" style="padding: 0.2rem 0.5rem;">Register</a> <!-- Reduced padding -->
                    </li>
                    <?php endif; ?>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <?php
        $flashMessages = getFlashMessages();
        foreach ($flashMessages as $flash): 
            $alertClass = $flash['type'] == 'error' ? 'danger' : 'success';
        ?>
            <div class="alert alert-<?php echo $alertClass; ?> alert-dismissible fade show">
                <?php echo htmlspecialchars($flash['message']); ?>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <?php endforeach; ?>

        <?php echo $content ?? ''; ?>
    </div>

    <footer class="mt-auto py-2 bg-dark text-white"> <!-- Reduced py-3 to py-2 -->
        <div class="container text-center">
            <p class="mb-0" style="font-size: 0.85rem;">© <?php echo date('Y'); ?> SunBeam Infotech Pvt. Ltd. - TodoApp. All rights reserved.</p> <!-- Reduced font size -->
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
